{
  "name": "weekme",
  "private": true,
  "scripts": {
    "prisma:seed": "turbo prisma:seed; npm run docker:down",
    "prisma:push": "turbo prisma:push; npm run docker:down",
    "prisma:generate": "turbo prisma:generate",
    "docker:up": "turbo docker:up",
    "docker:down": "turbo docker:down",
    "build": "turbo build",
    "dev": "turbo dev; npm run docker:down",
    "format": "prettier --write \"src/**/*.ts\" \"test/**/*.ts\""
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "turbo": "^2.3.3",
    "typescript": "5.7.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@10.9.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
